git config --global user.name <>
git config --global user.email <>

git config --list

git init

git status

git log -2

git commit -a (all changes should be committed) -m (message)

git remote -v

git push origin master



